import React from 'react';
import ErrorButton from './ErrorButton';

const NotFound = () => (
    <div>
        <ErrorButton message = "404 No Such Page Found.." />
    </div>
);

export default NotFound;